syntax = "proto3";
package eeta.eeta;

import "gogoproto/gogo.proto";
import "eeta/eeta/params.proto";

option go_package = "eeta/x/eeta/types";

// GenesisState defines the eeta module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
}
